/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ["cdn2.thecatapi.com",
    "images.unsplash.com"
  ],
  },
}

module.exports = nextConfig
